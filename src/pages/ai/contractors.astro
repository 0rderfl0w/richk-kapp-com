---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="AI Agents for Contractors | Richard Kappel" description="Done-for-you AI agents for trade contractors. Talk into your phone, upload photos, and a ready-to-send quote lands in your inbox in 5 minutes. You review it. You send it. Setup in 48 hours.">

  <!-- Hero -->
  <section class="relative overflow-hidden bg-gradient-to-br from-white via-brand-50/30 to-white">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 py-20 md:py-28">
      <div class="max-w-3xl mx-auto text-center animate-fade-in">
        <p class="text-brand-600 font-semibold text-sm tracking-widest uppercase mb-4">AI Agents for Trade Contractors</p>
        <h1 class="text-4xl md:text-5xl font-extrabold leading-tight text-gray-900">
          You are quoting jobs<br />by hand. That ends now.
        </h1>
        <p class="mt-6 text-lg text-gray-600 max-w-2xl mx-auto leading-relaxed">
          Talk into your phone. Upload the photos. A ready-to-send quote lands in your inbox in 5 minutes. You review it. You send it. Done-for-you. Set up in 48 hours.
        </p>
        <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/contact" class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-gradient-to-r from-brand-600 to-brand-700 text-white font-semibold rounded-lg hover:shadow-lg hover:shadow-brand-200 transition-all duration-300 transform hover:-translate-y-0.5 text-lg">
            Book a Free Demo →
          </a>
          <a href="#how-it-works" class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-white text-brand-600 font-semibold rounded-lg border border-brand-200 hover:border-brand-400 transition-all duration-300 text-lg">
            See How It Works
          </a>
        </div>
      </div>
    </div>
    <div class="absolute top-20 right-10 w-64 h-64 bg-brand-50 rounded-full blur-3xl opacity-40 -z-10"></div>
    <div class="absolute bottom-10 left-10 w-48 h-48 bg-brand-100/50 rounded-full blur-2xl opacity-30 -z-10"></div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- The Problem -->
  <section class="py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="grid md:grid-cols-2 gap-16 items-center">
        <div class="slide-in-left">
          <p class="text-brand-600 font-semibold text-sm tracking-widest uppercase mb-2">The Real Cost</p>
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900">Every quote takes an hour you're not billing.</h2>
          <p class="mt-6 text-gray-600 text-lg leading-relaxed">
            8 quotes a week. 45 minutes each. That's 6 hours of unpaid admin, every single week.
          </p>
          <p class="mt-4 text-gray-600 leading-relaxed">
            At $100/hour, you're losing $2,400 a month to paperwork. Not to overhead. Not to materials. To writing quotes by hand.
          </p>
          <p class="mt-4 text-gray-600 leading-relaxed">
            And the slow quote loses the job. The contractor who sends a professional quote within the hour wins. The one who sends it 3 days later gets ghosted.
          </p>
        </div>
        <div class="slide-in-right space-y-4">
          {[
            { label: '6 hrs/week', desc: 'Avg. admin time lost to manual quoting' },
            { label: '$2,400/mo', desc: 'Cost of that time at $100/hr billing rate' },
            { label: '3 days', desc: 'Avg. time competitors take to send a quote' },
            { label: '5 minutes', desc: 'Time to get a ready-to-send quote in your inbox' },
          ].map((item) => (
            <div class="bg-gradient-to-r from-white to-gray-50 rounded-2xl p-6 border border-gray-100 flex items-center gap-6 hover:border-brand-200 hover:shadow-sm transition-all duration-300">
              <span class="text-brand-600 font-bold text-lg whitespace-nowrap w-28 shrink-0">{item.label}</span>
              <p class="text-gray-600 text-sm">{item.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- How It Works -->
  <section id="how-it-works" class="py-20 relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-white via-brand-50/10 to-white -z-10"></div>
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="text-center max-w-3xl mx-auto mb-16 slide-in-up">
        <p class="text-brand-600 font-semibold text-sm tracking-widest uppercase mb-2">The Workflow</p>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900">Review-ready quote in 5 minutes.</h2>
        <p class="mt-4 text-gray-600 text-lg">No software to learn. No dashboard. No process to change.</p>
      </div>
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {[
          {
            step: '1',
            icon: '📸',
            title: 'Finish the walkthrough',
            desc: 'Pull out your phone at the job site. Same as always.',
            delay: '0s'
          },
          {
            step: '2',
            icon: '🎙️',
            title: 'Talk through the scope',
            desc: 'Describe the job, take photos of what needs doing. Voice or text.',
            delay: '0.1s'
          },
          {
            step: '3',
            icon: '💬',
            title: 'Send it to your agent',
            desc: 'Drop it into Telegram or WhatsApp. That\'s the whole input.',
            delay: '0.2s'
          },
          {
            step: '4',
            icon: '📧',
            title: 'Quote hits YOUR inbox',
            desc: 'A branded, ready-to-send quote lands in your inbox. 2 minutes to review. You hit send.',
            delay: '0.3s'
          },
        ].map((item) => (
          <div class="slide-in-up bg-white rounded-2xl p-8 border border-gray-100 hover:border-brand-200 hover:shadow-lg transition-all duration-300 text-center" style={`animation-delay: ${item.delay}`}>
            <div class="text-4xl mb-4">{item.icon}</div>
            <div class="w-8 h-8 bg-brand-600 text-white rounded-full flex items-center justify-center font-bold text-sm mx-auto mb-4">{item.step}</div>
            <h3 class="text-base font-bold text-gray-900">{item.title}</h3>
            <p class="mt-2 text-gray-500 text-sm leading-relaxed">{item.desc}</p>
          </div>
        ))}
      </div>
      <div class="mt-12 text-center slide-in-up" style="animation-delay: 0.4s">
        <p class="text-gray-500 text-base">Your agent knows your trade. It knows your pricing. It formats the quote the way you want it. Every time.</p>
      </div>
    </div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- The 5 Verticals -->
  <section class="py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="text-center max-w-3xl mx-auto mb-16 slide-in-up">
        <p class="text-brand-600 font-semibold text-sm tracking-widest uppercase mb-2">Who This Is Built For</p>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900">Built for the trades that quote constantly.</h2>
        <p class="mt-4 text-gray-600 text-lg">Each agent is trained for your trade. Not a generic tool. A specific one.</p>
      </div>
      <div class="space-y-4">
        {[
          {
            emoji: '🎨',
            trade: 'Painters',
            pain: '5 to 15 quotes a week. Every quote needs room measurements, surface condition, paint type, and labor. You\'re already taking photos on every job.',
            win: 'Agent extracts scope from your walkthrough. A review-ready quote hits your inbox in 5 minutes. You hit send.',
            delay: '0s'
          },
          {
            emoji: '🏠',
            trade: 'Roofers',
            pain: '$5K to $30K quotes written by hand. Photos are your whole assessment — damage, pitch, shingles, gutters. You\'re on a ladder with your phone already out.',
            win: 'Agent reads the photos, knows the difference between 3-tab and architectural. Quote ready before you\'re back on the ground.',
            delay: '0.05s'
          },
          {
            emoji: '💧',
            trade: 'Restoration Companies',
            pain: 'Water damage, mold, fire remediation. Photos are legally required. Insurance documentation needs to be precise. One wrong line item means a denied claim.',
            win: 'Agent builds insurance-grade documentation from your site photos and voice notes. 3-hour job becomes 5 minutes. You review, then send.',
            delay: '0.1s'
          },
          {
            emoji: '🌿',
            trade: 'Landscapers',
            pain: 'Every new client. Every spring. Walk the property, describe the scope, send a quote. Thin margins mean every hour of admin eats directly into profit.',
            win: 'Quote ready before you get back to the truck.',
            delay: '0.15s'
          },
          {
            emoji: '❄️',
            trade: 'HVAC',
            pain: '$3K to $15K installs. Unit sizing, ductwork, labor hours. Complex quotes with multiple line items. One missed component costs you $500 out of pocket.',
            win: 'Agent doesn\'t miss line items. Quote built from your voice notes and site photos. Every time.',
            delay: '0.2s'
          },
        ].map((item) => (
          <div class="slide-in-up bg-gradient-to-r from-white to-gray-50 rounded-2xl border border-gray-100 hover:border-brand-200 hover:shadow-md transition-all duration-300 overflow-hidden" style={`animation-delay: ${item.delay}`}>
            <div class="grid md:grid-cols-12 gap-0">
              <div class="md:col-span-3 p-8 flex items-center gap-4 border-b md:border-b-0 md:border-r border-gray-100">
                <span class="text-3xl">{item.emoji}</span>
                <h3 class="text-xl font-bold text-gray-900">{item.trade}</h3>
              </div>
              <div class="md:col-span-5 p-8 border-b md:border-b-0 md:border-r border-gray-100">
                <p class="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-2">The Problem</p>
                <p class="text-gray-600 text-sm leading-relaxed">{item.pain}</p>
              </div>
              <div class="md:col-span-4 p-8 bg-brand-50/30">
                <p class="text-xs font-semibold text-brand-600 uppercase tracking-wide mb-2">The Fix</p>
                <p class="text-gray-700 text-sm leading-relaxed font-medium">{item.win}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- The Offer -->
  <section class="py-20 relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-white via-brand-50/10 to-white -z-10"></div>
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="grid md:grid-cols-2 gap-16 items-center">
        <div class="slide-in-left">
          <p class="text-brand-600 font-semibold text-sm tracking-widest uppercase mb-2">The Offer</p>
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900">Done-for-you. 48-hour setup. 14-day guarantee.</h2>
          <p class="mt-6 text-gray-600 text-lg leading-relaxed">
            One 30-minute call. I build your agent, train it on your trade and pricing, and connect it to Telegram or WhatsApp.
          </p>
          <p class="mt-4 text-gray-600 leading-relaxed">
            First quote sent within 48 hours of onboarding. If it doesn't work the way I described, full refund. No questions.
          </p>
          <div class="mt-8">
            <a href="/contact" class="inline-flex items-center gap-2 px-8 py-4 bg-gradient-to-r from-brand-600 to-brand-700 text-white font-semibold rounded-lg hover:shadow-lg hover:shadow-brand-200 transition-all duration-300 transform hover:-translate-y-0.5 text-lg">
              Book a Free Demo →
            </a>
          </div>
        </div>
        <div class="slide-in-right space-y-6">
          <div class="bg-white rounded-2xl p-8 border border-gray-100 shadow-sm">
            <div class="flex items-baseline gap-2 mb-1">
              <span class="text-4xl font-extrabold text-gray-900">$597</span>
              <span class="text-gray-400 font-medium">one-time setup</span>
            </div>
            <div class="flex items-baseline gap-2 mb-6">
              <span class="text-2xl font-bold text-brand-600">$97</span>
              <span class="text-gray-400 font-medium">/month after</span>
            </div>
            <ul class="space-y-3">
              {[
                'Custom agent built for your trade',
                'Telegram or WhatsApp integration',
                'Branded quote template',
                '30-min onboarding call',
                'First quote sent within 48 hours',
                '14-day money-back guarantee',
                'Cancel anytime',
              ].map((item) => (
                <li class="flex items-center gap-3 text-gray-700 text-sm">
                  <div class="w-5 h-5 bg-brand-50 rounded-full flex items-center justify-center shrink-0">
                    <svg class="w-3 h-3 text-brand-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                  {item}
                </li>
              ))}
            </ul>
          </div>
          <div class="bg-gradient-to-r from-brand-600 to-brand-700 rounded-2xl p-6 text-white shadow-lg shadow-brand-200/30">
            <p class="text-sm font-semibold opacity-80 mb-1">The math</p>
            <p class="text-lg font-bold">6 hrs/week x $100/hr = $2,400/mo in lost time</p>
            <p class="text-sm opacity-90 mt-1">You're spending $97 to get it back.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- Why Me -->
  <section class="py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="grid md:grid-cols-5 gap-12 items-start">
        <div class="md:col-span-2 slide-in-left md:sticky md:top-24">
          <p class="text-brand-600 font-semibold text-sm tracking-widest uppercase mb-2">Why Me</p>
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900 leading-tight">I built this. I use this.</h2>
          <p class="mt-4 text-gray-500 leading-relaxed">
            Not theorizing. Not prompting a demo. Running it live.
          </p>
        </div>
        <div class="md:col-span-3 space-y-0">
          <div class="relative pl-8 pb-12 border-l-2 border-brand-100 slide-in-right group" style="animation-delay: 0s">
            <div class="absolute left-0 top-0 w-4 h-4 bg-brand-600 rounded-full -translate-x-[9px] group-hover:scale-125 transition-transform duration-300"></div>
            <p class="text-brand-600 font-bold text-xs tracking-widest uppercase">Practitioner</p>
            <h3 class="text-xl font-bold text-gray-900 mt-2">I run the exact agent I'm selling you.</h3>
            <p class="mt-2 text-gray-600 leading-relaxed">This isn't a concept. I built it, deployed it, and I know exactly where it breaks at 11pm on a Tuesday. That knowledge is built into every agent I set up for clients.</p>
          </div>
          <div class="relative pl-8 pb-12 border-l-2 border-brand-100 slide-in-right group" style="animation-delay: 0.15s">
            <div class="absolute left-0 top-0 w-4 h-4 bg-brand-600 rounded-full -translate-x-[9px] group-hover:scale-125 transition-transform duration-300"></div>
            <p class="text-brand-600 font-bold text-xs tracking-widest uppercase">Community-Tested</p>
            <h3 class="text-xl font-bold text-gray-900 mt-2">Patterns from hundreds of operators.</h3>
            <p class="mt-2 text-gray-600 leading-relaxed">I lead Build to Own Club, a community of founders and operators replacing manual workflows with AI. Every edge case, every workaround, every win gets fed back into every agent I build.</p>
          </div>
          <div class="relative pl-8 slide-in-right group" style="animation-delay: 0.3s">
            <div class="absolute left-0 top-0 w-4 h-4 bg-brand-600 rounded-full -translate-x-[9px] group-hover:scale-125 transition-transform duration-300"></div>
            <p class="text-brand-600 font-bold text-xs tracking-widest uppercase">20 Years Deep</p>
            <h3 class="text-xl font-bold text-gray-900 mt-2">I understand business before I touch the tech.</h3>
            <p class="mt-2 text-gray-600 leading-relaxed">Two decades building and running companies. I know what a painter's quote needs to include. I know how insurance adjusters read a restoration report. That context is the difference between an agent that works and one that embarrasses you.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- Final CTA -->
  <section class="py-20 relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-white via-brand-50/10 to-white -z-10"></div>
    <div class="max-w-3xl mx-auto px-4 sm:px-6 text-center slide-in-up">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900">Book a 20-minute demo.</h2>
      <p class="mt-4 text-gray-600 text-lg leading-relaxed">
        I'll show you the full quote workflow live. You'll see exactly what your client receives. If it's not exactly what you need, we don't move forward.
      </p>
      <a href="/contact" class="mt-8 inline-flex items-center gap-2 px-10 py-5 bg-gradient-to-r from-brand-600 to-brand-700 text-white font-semibold rounded-lg hover:shadow-xl hover:shadow-brand-200 transition-all duration-300 transform hover:-translate-y-0.5 text-xl">
        Book a Free Demo →
      </a>
      <p class="mt-4 text-sm text-gray-400">No commitment. No sales pitch. Just the workflow.</p>
    </div>
  </section>

  <!-- Divider -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6"><div class="h-px bg-gradient-to-r from-transparent via-brand-200 to-transparent"></div></div>

  <!-- Build to Own Club (bottom, soft) -->
  <section class="py-16">
    <div class="max-w-4xl mx-auto px-4 sm:px-6">
      <div class="bg-gradient-to-br from-white to-brand-50/40 rounded-2xl p-8 border border-brand-100/60 flex flex-col md:flex-row items-center gap-6 slide-in-up">
        <div class="flex-1 text-center md:text-left">
          <p class="text-brand-600 font-semibold text-xs tracking-widest uppercase mb-1">Community</p>
          <h3 class="text-xl font-bold text-gray-900">Build to Own Club</h3>
          <p class="mt-2 text-gray-600 text-sm leading-relaxed">
            Want to build your own AI agents? Join a growing community of founders and operators building and owning their own automated workflows.
          </p>
        </div>
        <a href="/contact" class="shrink-0 inline-flex items-center gap-2 px-6 py-3 bg-white text-brand-600 font-semibold rounded-lg border border-brand-200 hover:border-brand-400 hover:shadow-sm transition-all duration-300 whitespace-nowrap">
          Learn More →
        </a>
      </div>
    </div>
  </section>

</Layout>

<style>
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in {
    animation: fadeIn 0.8s ease-out;
  }

  .slide-in-left,
  .slide-in-right,
  .slide-in-up {
    opacity: 0;
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }
  .slide-in-left {
    transform: translateX(-40px);
  }
  .slide-in-right {
    transform: translateX(40px);
  }
  .slide-in-up {
    transform: translateY(30px);
  }
  .slide-in-left.visible,
  .slide-in-right.visible,
  .slide-in-up.visible {
    opacity: 1;
    transform: translate(0);
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.15, rootMargin: '0px 0px -50px 0px' }
  );

  document.querySelectorAll('.slide-in-left, .slide-in-right, .slide-in-up').forEach((el) => {
    observer.observe(el);
  });
</script>
